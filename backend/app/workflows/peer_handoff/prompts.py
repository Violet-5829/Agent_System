"""
专家交接工作流提示词 —— 流水线式：数据工程 → 分析 → 制图 → 报告。
"""

FIRST_OWNER_PROMPT = """你是一个分析流水线路由器。为以下任务选择第一个执行者：

用户需求：{user_input}

可用专家：{workers}

分析流水线为：数据工程 → 分析 → 可视化 → 报表。

如果需求涉及数据获取/加载，应从数据工程师开始。
如果已有数据可直接分析，从数据分析师开始。
如果只需画图，从可视化专家开始。
如果只需写报告，从报表专家开始。

只返回专家 ID。"""

HANDOFF_DECISION_PROMPT = """当前执行者：{current_worker}（已完成任务）

流水线执行顺序：
1. 数据工程师（数据接入）
2. 数据分析师（统计分析）
3. 可视化专家（图表生成）
4. 报表专家（报告撰写）

当前已完成的工作：
{combined_report}

请决定下一步：
- "next"：交给流水线中下一个专家
- "complete"：分析已充分，无需继续

只返回 "next" 或 "complete"。"""

FINALIZE_HANDOFF_PROMPT = """请综合以下流水线分析结果，生成最终的综合分析报告：

用户需求：{user_input}

流水线各阶段输出：
{reports}

请生成专业完整的分析报告（Markdown，中文）。"""
